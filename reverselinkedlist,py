head=[1,2,3,4,5]
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def reverseList(self, head) :
        t = head
        p = None
        while head:
            temp = head
            head = head.next
            temp.next = None
            temp.next = p
            p = temp
        return p

